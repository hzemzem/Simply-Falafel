Uses motdatla package for storing .env information.  Good for storing unique usernames and passwords for our local mySQL

create a file with anyname.env

For our project, the only thing you need to fill is:

---

DB_PASS=password
DB_USER=root

---